# Engineering workflow

## 目的

このリポジトリでは、変更を `dev`、`preview`、`main` の順に段階的に昇格する。
各ブランチへのPull Request（PR）のマージを、そのブランチに対応する環境への
デプロイトリガーとする。コードの統合、環境上での確認、次段階への昇格を分離し、
開発途中の変更を誤って公開しないことを目的とする。

この文書は目標とする運用契約を定義する。自動化が未実装または停止している場合、
ブランチが更新されただけで環境も更新されたとはみなさない。

## ブランチと環境

| ブランチ  | 役割                                   | 対応する環境                                 |
| --------- | -------------------------------------- | -------------------------------------------- |
| `dev`     | 既定ブランチ。開発中の変更を統合する   | dev環境（`dev-video.soramimic.com`）         |
| `preview` | 公開候補として選んだ変更だけを保持する | preview環境（`preview-video.soramimic.com`） |
| `main`    | 公開版のソース・オブ・トゥルース       | production環境（`video.soramimic.com`）      |

通常のPRの向きは次の3種類に限定する。

```text
feature/fix branch ──PR──> dev ──選択的なPR──> preview ──release PR──> main
                              │                  │                    │
                              ▼                  ▼                    ▼
                         dev deploy         preview deploy       production deploy
```

## 1. 開発: feature/fixからdevへ

1. 作業ごとにfeature/fixブランチを作る。
2. `dev`をbaseにPRを作る。
3. 必須チェックが成功したらPRを自動マージする。
4. マージされた正確なcommit SHAをdev環境へ自動デプロイする。
5. 開発者がdev環境で、機能として問題がないか確認する。

実装作業の完了条件は、原則としてPRが`dev`へマージされることとする。ただし、
`preview`へ昇格できることを意味するのは、dev環境へのデプロイ成功と動作確認も
完了した後である。

## 2. 公開候補の選択: devからpreviewへ

`dev`と`preview`は常に同期させない。`dev`には未完成、検証中、または今回公開しない
変更が含まれ得るため、`dev`全体を`preview`へマージしない。

1. 開発者がdev環境で確認済みの変更から、公開候補にする変更を明示的に選ぶ。
2. 最新の`preview`からpromotionブランチを作る。
3. 対象のdev PRに対応するcommitだけをcherry-pickする。依存する変更がある場合は、
   依存関係をすべて含めるか、昇格を見送る。
4. promotionブランチから`preview`へ、対象変更を列挙したPRを作る。
5. 明示的な昇格指示があり、必須チェックが成功したらPRを自動マージする。
6. マージされた正確なcommit SHAをpreview環境へ自動デプロイする。
7. 開発者またはリリース判断者がpreview環境で、公開候補として問題がないか確認する。

CI成功だけ、スケジュール、または一般的な「完了して」という指示は、変更を
`preview`へ昇格する承認とはみなさない。どの変更を昇格するかが明示されている必要がある。

## 3. 公開: previewからmainへ

通常運用では、`main`をbaseにするPRのheadは`preview`だけにする。feature/fixブランチや
`dev`から`main`へ直接PRを出さない。

1. preview環境で、現在の`preview`のcommit SHAと公開予定の変更一覧を確認する。
2. 動作確認後、リリース判断者がその内容の公開を明示的に指示する。
3. `preview`から`main`へのrelease PRを作る。
4. 必須チェックが成功したらPRを自動マージする。
5. マージされた正確なcommit SHAをproduction環境へ自動デプロイする。
6. health/readiness、主要動線、および公開中のcommit SHAを確認する。

release PRの作成、マージ、およびproductionデプロイは、preview環境を確認した後の
明示的なリリース指示がなければ行わない。

## PRマージとデプロイの契約

各常設ブランチへのPRマージと対応環境へのデプロイは、運用上ひとつの昇格操作として
扱う。実装する自動化は次を満たすこと。

- ブランチへのpushではなく、保護されたPRのマージ結果をデプロイ対象にする。
- マージされたcommit SHAを固定し、その同じSHAをビルド、検証、デプロイする。
- 同じSHAのデプロイを安全に再実行できるようにする。
- 各環境で現在稼働中のcommit SHAとデプロイ結果を確認できるようにする。
- デプロイ失敗時は成功として扱わず、直前の正常版を維持または復元し、失敗を通知する。
- 次のブランチへ昇格する前に、現在の環境が対象SHAで正常であることを確認する。

PRがマージされた時点で開発上の統合は完了する。一方、環境への昇格はデプロイが
成功するまで完了しない。ブランチと環境のSHAが一致しない状態は障害として扱い、
その状態のまま次段階へ進めない。

## 自動マージと承認の境界

自動マージは、必要な判断を省略する仕組みではなく、判断後にCI完了を待って機械的に
マージする仕組みとする。

- `dev`: 通常の開発PRは、必須チェック成功後に自動マージできる。
- `preview`: 昇格する変更を明示した指示があったPRだけ、自動マージできる。
- `main`: preview環境を確認した後、そのリリースを明示した指示があったPRだけ、
  自動マージできる。

これにより、日常の開発は`dev`まで自動で進めつつ、公開範囲の選択と本番公開の判断は
人が段階ごとに行う。
